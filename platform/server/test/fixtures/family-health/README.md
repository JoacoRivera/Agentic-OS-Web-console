# Fixture: Family Health record

Synthetic, invented data for the console's tests (ADR-0010). Nothing here describes a
real person. Mirrors the Health-Management layout: `family-overview.md`, one folder per
member with `profile.md`, `history.md`, `exams/`, `documents/`, and shared `reference/`.
