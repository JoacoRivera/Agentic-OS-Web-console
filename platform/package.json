{
  "name": "agentic-os-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Agentic OS web console (Odysseus) — localhost-only private console over the memory repo",
  "workspaces": [
    "server",
    "dashboard"
  ],
  "scripts": {
    "dev": "node --env-file-if-exists=.env scripts/dev.mjs",
    "build": "npm run build --workspace dashboard",
    "start": "node --env-file-if-exists=.env server/src/index.js",
    "test": "npm test --workspace server",
    "verify": "node --env-file-if-exists=.env scripts/verify.mjs",
    "check:paths": "node scripts/check-paths.mjs",
    "check:docs": "node --env-file-if-exists=.env scripts/check-docs.mjs",
    "check:workflows": "node --env-file-if-exists=.env scripts/check-workflows.mjs",
    "check:skills": "node --env-file-if-exists=.env scripts/check-skills.mjs",
    "check:metrics-groundtruth": "node --env-file-if-exists=.env scripts/check-metrics-groundtruth.mjs"
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  },
  "engines": {
    "node": ">=22.9"
  }
}
